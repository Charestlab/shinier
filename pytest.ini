# pytest.ini
[pytest]
minversion = 7.0
addopts = -ra -q
testpaths =
    tests/unit_tests
    tests/validation_tests
markers =
    unit_tests: default functional
    validation_tests: full combinatorial sweep (slow)
