# pytest.ini
[pytest]
minversion = 7.0
addopts = -ra -q
testpaths =
    tests/unit_tests
    tests/validation_tests
markers =
    smoke: ultra-fast checks
    core: default functional
    exhaustive: full combinatorial sweep (slow)
